name = "my-portfolio-chatbot"
main = "worker.js"
compatibility_date = "2024-01-01"

# After deploying, your worker URL will be:
# https://my-portfolio-chatbot.<your-subdomain>.workers.dev
#
# Rename "name" above to something unique (e.g. "eren-portfolio-bot")
# and update CONFIG.apiEndpoint in public/js/config.js to match.
#
# Set your secret:  wrangler secret put GROQ_API_KEY
